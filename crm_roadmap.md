# 🗺️ מפת הדרכים של מערכת ה-CRM (autoRI-studio)

מסמך זה מתאר את שלבי הפיתוח, המצב הנוכחי ותוכניות הפיתוח העתידיות עבור מערכת ה-CRM של **autoRI-studio**.

---

## 🎯 חזון ומטרות על
מערכת ה-CRM של autoRI-studio משמשת כמרכז העצבים לניהול לידים, פרויקטים, אוטומציות ובקרת תקציבים/מכסות (SLA) בזמן אמת. מטרת העל היא לספק פלטפורמה אמינה, שקופה ובעלת ערך כלכלי מוכח ללקוחות הסטודיו (ROI).

---

## 🗺️ שלבי פיתוח ומצב נוכחי

### 🟢 שלב 1: תשתית וליבת המערכת (הושלם)
*   **בסיס נתונים (Supabase Schema):** הקמת טבלאות לידים, משימות, הערות, אוטומציות, באגים ולוגים.
*   **לוח קנבן (Kanban Board):** מעקב אחר תהליך המכירה, גרירת לידים וניהול סטטוסים.
*   **כרטיס לקוח מורחב (LeadDetailsModal):** איסוף נתוני השאלון האינטראקטיבי והצגת שיחות הבוט מול הליד.
*   **ניהול משימות חכם:** מנגנון גאנט בסיסי למניעת סימון משימה כהושלמה אם יש משימה חוסמת (`dependency_id`).

### 🟢 שלב 2: ניהול אוטומציות ובקרת מכסות (הושלם)
*   **מנגנון הגבלת ריצות (Quota Capping):** פונקציות RPC ב-Supabase וחסימה/חיוב יתר בזמן אמת ב-N8N.
*   **דיאגנוסטיקה טכנית מורחבת:** הצגת לוגי ריצות, פענוח שגיאות אוטומטי מבוסס כללים וחישוב ציון בריאות (Health Score) לאוטומציה.
*   **ארכוב וסיכום יומי (Daily Rollups):** פונקציות ניקוי (Purge) וארכוב למניעת עומס במסד הנתונים.
*   **ניטור תקלות אקטיבי:** אינטגרציית מנטר השגיאות (Error Monitor Workflow) לשליחת התראות Gmail ו-Supabase בעת קריסה.

### 🟢 שלב 3: מחשבון ROI ותמחור משודרג (הושלם)
*   **מחשבון תמחור היברידי:** תמיכה מלאה במעבר בין פרויקטי אוטומציה/AI לבין בניית אתרי אינטרנט לעסקים.
*   **תמחור אתרים מותאם:** שילוב סוגי אתרים, רכיבי Add-ons (צ'אטבוט Gemini, שאלון, מחשבון, חיבור CRM) ותחזוקת שרתים חודשית (SLA).
*   **ייצוא הצעות מחיר:** הפקת הצעת מחיר מפורמטת היטב בעברית עסקית לשיתוף מהיר ושמירה בכרטיס הליד.
*   **הצגת נתונים משופרת:** כרטיס הליד מציג דינמית את מפרט האתר והתוספות שנרכשו.

### 🟡 שלב 4: אוטומציה של מסמכים וחוזים (בפיתוח)
*   **מחולל מסמכים אוטומטי:** שילוב API להפקה אוטומטית של קבצי PDF (הצעות מחיר, חוזים וחשבוניות) המבוססים ישירות על נתוני המחשבון השמורים בכרטיס הליד.
*   **חתימה דיגיטלית:** שליחת חוזים לחתימה אוטומטית דרך מערכות חיצוניות (כמו DocuSign או ספקי חתימה דיגיטלית מקומיים) והעלאה ישירה של החוזה החתום ל-Supabase Storage.

### 🔵 שלב 5: חיבורים ותקשורת רב-ערוצית (מתוכנן)
*   **שילוב WhatsApp Business API:** שליחת תזכורות משימות, הצעות מחיר ועדכוני סטטוס ישירות לוואטסאפ של הלקוח.
*   **אינטגרציית Gmail ויומנים:** סנכרון אוטומטי של פגישות אפיון שנולדו מהצ'אטבוט/שאלון ישירות ליומן Google Calendar.

### 🔵 שלב 6: דוחות מתקדמים ובינה עסקית - BI (מתוכנן)
*   **לוח מחוונים פיננסי למנהל:** מעקב אחר הכנסות שוטפות (MRR), רווחיות פרויקטים, ותחזית הכנסות.
*   **דוחות שימוש אינטראקטיביים ללקוח (Client Portal):** אזור לקוח מאובטח המאפשר לו לצפות בסטטיסטיקות הריצה של האוטומציות שלו, היסטוריית תשלומים, ופתיחת קריאות שירות (באגים).

---
*מפת דרכים זו היא דינמית ומעודכנת לפי קצב פיתוח הסטודיו. autoRI-studio 🚀*
