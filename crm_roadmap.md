# 🗺️ מפת הדרכים לאוטומציית ה-CRM של autoRI-studio

מסמך זה מהווה ייצוא רשמי ומדויק של **מפת הדרכים לאוטומציה** מתוך מערכת ה-CRM.

---

## 📊 סיכום התקדמות כללי
*   **אחוז השלמה:** 35% (5 מתוך 14 רכיבים הושלמו)
*   **רכיבים שהושלמו:** 5
*   **רכיבים בפיתוח:** 1
*   **רכיבים מתוכננים:** 8

---

## 📋 פירוט רכיבי מפת הדרכים

### 1. בקרת הרצות וחסימת מכסות (Runs Capping)
*   **סטטוס:** 🟢 הושלם (100%)
*   **קטגוריה:** מכירות וסליקה
*   **תיאור:** מנגנון להגבלת הרצות אוטומטיות של הבוטים בהתאם לחבילת הבסיס ותוספות הבונוס של הלקוח.
*   **מנגנון אינטגרציה טכני:** פונקציית PostgreSQL RPC בשם `check_automation_quota` המשולבת בתוך N8N לפני הרצת Gemini, וחוסמת או מנתבת את הריצה בהתאם.
*   **תועלת עסקית:** מניעת חריגות תקציב של מפתחות ה-API ומניעת הפסדים כספיים לחברה.
*   **טבלאות Supabase מושפעות:** `automations`, `automation_runs`, `check_automation_quota` RPC.
*   **צמתי N8N נדרשים:** צומת HTTP Request ל-RPC, קוביית If (`Quota Allowed?`), צומת Log Run וצומת Respond.

---

### 2. דיאגנוסטיקה מונחית AI וניתוח תקלות
*   **סטטוס:** 🟢 הושלם (100%)
*   **קטגוריה:** תפעול וניטור שרתים
*   **תיאור:** פענוח שגיאות ריצה מתוך N8N: ניתוח מבוסס חוקים לבעיות נפוצות + כפתור דיאגנוסטיקה חיה בעזרת Gemini Flash.
*   **מנגנון אינטגרציה טכני:** ה-CRM שולף את מבנה ה-Workflow שנכשל ישירות משרת ה-N8N (דרך Vite CORS Proxy), מזין אותו ל-Gemini Flash ומקבל הסבר והנחיות לתיקון בעברית.
*   **תועלת עסקית:** קיצור זמן הטיפול בתקלות (MTTR) מאינטגרציות מורכבות ללא צורך בכניסה ידנית ללוגים של N8N.
*   **טבלאות Supabase מושפעות:** `automation_runs`, `system_alerts`.
*   **צמתי N8N נדרשים:** שימוש ב-N8N Core API לגישה לנתוני ה-Workflow והשלבים.

---

### 3. אגרגציה וצמצום נתונים (Daily Rollups)
*   **סטטוס:** 🟢 הושלם (100%)
*   **קטגוריה:** תפעול וניטור שרתים
*   **תיאור:** מנגנון אוטומטי המאגד הרצות בודדות לסיכומים יומיים שבועיים ושנתיים, ומנקה היסטוריית ריצות ישנות לחיסכון במקום.
*   **מנגנון אינטגרציה טכני:** פונקציית SQL מתוזמנת שמסכמת את אתמול לטבלת `daily_stats`, מוחקת הצלחות ישנות מ-7 ימים ושגיאות מ-30 יום.
*   **תועלת עסקית:** חיסכון של למעלה מ-90% בנפח האחסון של Supabase ומניעת איטיות בגרפים ובממשק.
*   **טבלאות Supabase מושפעות:** `automation_daily_stats`, `automation_runs`, `run_daily_automation_rollup` function.
*   **צמתי N8N נדרשים:** סימולציה מובנית ב-CRM למצב אופליין, וג'וב מתוזמן (Cron) ב-Supabase.

---

### 4. חסימת שלבים מבוססת מסמכים (Hard Gating)
*   **סטטוס:** 🟢 הושלם (100%)
*   **קטגוריה:** מכירות וסליקה
*   **תיאור:** מניעת מעבר שלבים במכירות ובפרויקטים ללא העלאת מסמכי איכות וחתימות (NDA, חוזה, spec טכני, כרטיס גישות).
*   **מנגנון אינטגרציה טכני:** ולידציה בפרונטאנד הבודקת את קיומם של קבצים משויכים בטבלת `documents` לפני שינוי הסטטוס ב-Supabase.
*   **תועלת עסקית:** שמירה על סדר עבודה קפדני, מניעת מעבר לפיתוח ללא חוזה חתום, ומניעת מעבר ל-LIVE ללא אפיון.
*   **טבלאות Supabase מושפעות:** `documents`, `leads`, `automations`.
*   **צמתי N8N נדרשים:** אין צורך, מיושם ישירות ברמת ה-Client.

---

### 5. אבטחה ואוטנטיקציית משתמשים (User Authentication)
*   **סטטוס:** 🟡 בפיתוח (30%)
*   **קטגוריה:** ליבת המערכת ואבטחה
*   **תיאור:** מערכת התחברות (Login) ורישום מאובטחת המבוססת על Supabase Auth. המערכת תגביל את הגישה ל-CRM רק לאנשי הצוות המורשים בארגון.
*   **מנגנון אינטגרציה טכני:** שימוש במנגנון ה-JWT של Supabase להגדרת חוקי RLS מפורשים (Row Level Security) בטבלאות - כך שמשתמשים שאינם מחוברים ייחסמו טוטאלית.
*   **תועלת עסקית:** אבטחת מידע ארגוני רגיש ומניעת גישה לא מורשית ללידים, מפתחות API, ולנתוני הרצה.
*   **טבלאות Supabase מושפעות:** `storage.buckets` (docs), `public.users`, `public.leads`, ופוליסיס לכלל הטבלאות.
*   **צמתי N8N נדרשים:** אין צורך, הגדרה ברמת ה-React App וה-Supabase Client.

---

### 6. מחולל מסמכים וחתימות דיגיטליות (Auto-Doc & E-Sign)
*   **סטטוס:** 🔵 מתוכנן (0%)
*   **קטגוריה:** מכירות וסליקה
*   **תיאור:** יצירה אוטומטית של הצעות מחיר, NDA, וחוזים בפורמט PDF על בסיס נתוני הליד ושילוב מנגנון חתימה דיגיטלית ב-WhatsApp/Email.
*   **מנגנון אינטגרציה טכני:** קוביית N8N המאזינה לשינוי שלב ב-CRM, מרכיבה PDF באמצעות HTML template, ושולחת אותו דרך API של שירות חתימות (כמו DocuSign או SignNow).
*   **תועלת עסקית:** קיצור זמן סגירת העסקאות (Sales Cycle) וביטול הצורך ביצירת מסמכים ידנית.
*   **טבלאות Supabase מושפעות:** `documents`, `leads` (עדכון סטטוס אוטומטי בחתימה).
*   **צמתי N8N נדרשים:** מאזין ל-Supabase Webhook ➔ קוביית PDF Generator ➔ קוביית E-Sign API ➔ שליחת קישור ב-WhatsApp.

---

### 7. הפקת חשבוניות וסליקה לפי שימוש (Metered Billing)
*   **סטטוס:** 🔵 מתוכנן (0%)
*   **קטגוריה:** מכירות וסליקה
*   **תיאור:** חישוב אוטומטי של עלות הריטיינר החודשי + עלויות החריגה (Overage) בסוף החודש, הפקת חשבונית ושליחת קישור לסליקה.
*   **מנגנון אינטגרציה טכני:** וורקפלו מתוזמן ב-N8N הסורק את כמות הריצות בטבלת `automation_daily_stats`, מחשב חריגות מול ה-`runs_goal`, פונה ל-API של מערכת חשבוניות ומחולל קישור סליקה של Stripe.
*   **תועלת עסקית:** חיוב מדויק ואוטומטי ללא התעסקות של הנהלת חשבונות ידנית בסוף החודש.
*   **טבלאות Supabase מושפעות:** `automation_daily_stats`, `automations` (`runs_goal`, `custom_overage_rate`).
*   **צמתי N8N נדרשים:** טריגר תזמון חודשי ➔ שליפת נתונים מ-Supabase ➔ חישוב מתמטי ב-Code node ➔ הפקת חשבונית ➔ Stripe payment link ➔ שליחה ב-WhatsApp.

---

### 8. פורטל לקוחות בשירות עצמי (Client Portal)
*   **סטטוס:** 🔵 מתוכנן (0%)
*   **קטגוריה:** שירות עצמי ותמיכה
*   **תיאור:** פורטל לקוחות ייעודי ומאובטח המאפשר לכל לקוח לראות את קצב הריצות שלו, מסמכים לחתימה, סטטוס הפיתוח, ודיווח על תקלות.
*   **מנגנון אינטגרציה טכני:** יצירת דף כניסה נפרד ב-Vite המאומת ע"י מפתח גישה ייחודי ללקוח (Token). הפורטל מושך מידע מוגבל בלבד מתוך Supabase (ללא חשיפת לקוחות אחרים).
*   **תועלת עסקית:** חוסך עשרות שעות שירות לקוחות ומשפר את אמון ושביעות רצון הלקוח.
*   **טבלאות Supabase מושפעות:** `leads`, `automations`, `automation_daily_stats`, `documents`, `bugs`.
*   **צמתי N8N נדרשים:** אין צורך, מיושם ברמת ה-CRM Client.

---

### 9. מחולל שלד N8N מאפיון טכני (Spec-to-Workflow AI)
*   **סטטוס:** 🔵 מתוכנן (0%)
*   **קטגוריה:** ליבת המערכת ואבטחה
*   **תיאור:** קריאת מסמך האפיון הטכני (Spec) ע"י בינה מלאכותית, והורדת קובץ JSON של Workflow מוכן ל-N8N המכיל את כל החיבורים הנדרשים.
*   **מנגנון אינטגרציה טכני:** פנייה ל-Gemini Flash המנתח את קובץ האפיון, מזהה את ה-triggers וה-actions, בונה מבנה JSON תקין של N8N ומציג כפתור הורדה.
*   **תועלת עסקית:** חיסכון עצום בזמני הפיתוח הראשוניים של האוטומציות והפחתת טעויות אנוש.
*   **טבלאות Supabase מושפעות:** `documents`, `automations`.
*   **צמתי N8N נדרשים:** שליפת נתוני האפיון וייצור ה-Workflow JSON.

---

### 10. תיקון תקלות עצמי אוטומטי (Self-Healing)
*   **סטטוס:** 🔵 מתוכנן (0%)
*   **קטגוריה:** תפעול וניטור שרתים
*   **תיאור:** זיהוי כשלים קריטיים בריצות (כגון שגיאות Timeout או OAuth expired) והפעלת תהליכי התאוששות ותיקון אוטומטיים.
*   **מנגנון אינטגרציה טכני:** מנטר השגיאות N8N מזהה כשל ➔ פונה ל-API של N8N לביצוע Retry (במידה וזו בעיית רשת) או שולח הודעת WhatsApp ללקוח עם קישור להתחברות מחדש (במידה ותוקף החיבור פג).
*   **תועלת עסקית:** מניעת השבתת מערכות ממושכת וצמצום של כ-50% בפניות תמיכה טכנית.
*   **טבלאות Supabase מושפעות:** `automation_runs`, `system_alerts`.
*   **צמתי N8N נדרשים:** Error Trigger ➔ ניתוח שגיאה ב-Code Node ➔ ניסיון הפעלה מחדש (Retry HTTP API) או שליחת קישור שחזור ללקוח.

---

### 11. ניטור שרתים והפעלה מחדש עצמית (Server Monitor)
*   **סטטוס:** 🔵 מתוכנן (0%)
*   **קטגוריה:** תפעול וניטור שרתים
*   **תיאור:** מעקב שוטף אחרי זמינות שרת ה-N8N וזמינות ה-Workflows. הפעלה מחדש של Workflows שנכבו בטעות וקריאת חירום במקרה של קריסה.
*   **מנגנון אינטגרציה טכני:** סקריפט ב-N8N או CRM המאזין ל-Active state של ה-Workflows, ומבצע קריאת API להפעלתם מחדש. במקרה של חוסר תגובה מהשרת - הפעלת התראת שיחת טלפון.
*   **תועלת עסקית:** שרידות (Uptime) מקסימלית של מערכות הייצור ומענה מיידי לקריסות שרת.
*   **טבלאות Supabase מושפעות:** `system_alerts`.
*   **צמתי N8N נדרשים:** Interval Trigger ➔ בדיקת סטטוס שרת N8N ➔ בדיקת Active/Inactive ➔ במידת הצורך קריאת Twilio Call API.

---

### 12. מערכת קליטת לקוח אוטומטית (Automated Onboarding)
*   **סטטוס:** 🔵 מתוכנן (0%)
*   **קטגוריה:** שירות עצמי ותמיכה
*   **תיאור:** פתיחת תיקיית לקוח ב-Google Drive, ערוץ Slack/Discord ייעודי ללקוח, ושליחת ערכת הצטרפות וסרטוני הדרכה אוטומטית.
*   **מנגנון אינטגרציה טכני:** וורקפלו ב-N8N המופעל בעת מעבר ליד ל-Won ➔ יצירת תיקייה ב-Drive ➔ פנייה ל-Slack API לפתיחת ערוץ ➔ שליחת הודעת וואטסאפ ללקוח עם קישורים והדרכה.
*   **תועלת עסקית:** חוסך זמן מנהלתי יקר, מעניק רושם ראשוני מקצועי מאוד ללקוח החדש.
*   **טבלאות Supabase מושפעות:** `leads` (סטטוס won).
*   **צמתי N8N נדרשים:** Supabase Webhook ➔ Google Drive node ➔ Slack node ➔ WhatsApp Send node.

---

### 13. סקר שביעות רצון ואיסוף ביקורות (Auto CSAT)
*   **סטטוס:** 🔵 מתוכנן (0%)
*   **קטגוריה:** שירות עצמי ותמיכה
*   **תיאור:** שליחת סקרי שביעות רצון (CSAT) אוטומטיים ללקוחות לאחר עלייה לאוויר וניתוב לקבלת ביקורות Google Business חיוביות.
*   **מנגנון אינטגרציה טכני:** שליחת הודעת WhatsApp אינטראקטיבית 30 יום לאחר עליית אוטומציה ל-Live. דירוג 9-10 מנתב לבקשת ביקורת בגוגל, דירוג מתחת ל-7 פותח באג דחוף ב-CRM.
*   **תועלת עסקית:** שיפור מתמיד באיכות, קבלת ביקורות חיוביות בגוגל, וזיהוי מוקדם של לקוחות מאוכזבים.
*   **טבלאות Supabase מושפעות:** `leads`, `bugs` (פתיחת באג במקרה של דירוג נמוך).
*   **צמתי N8N נדרשים:** Timer Trigger (30 days from live) ➔ WhatsApp interactive message ➔ conditional logic ➔ Google link / CRM Bug log.

---

### 14. זיהוי סיכוני נטישה ואופטימיזציה (Churn Detection)
*   **סטטוס:** 🔵 מתוכנן (0%)
*   **קטגוריה:** שירות עצמי ותמיכה
*   **תיאור:** ניתוח דפוסי שימוש בריצות האוטומציה. התרעה על ירידות חדות בשימוש או ריבוי שגיאות המעידים על סיכון נטישת לקוח.
*   **מנגנון אינטגרציה טכני:** סקריפט SQL/N8N המנתח את סך הריצות השבועי. ירידה של מעל 50% בנפח הריצות או שגיאות חוזרות במשך 3 ימים יוצרות התראה "Churn Risk" ב-CRM.
*   **תועלת עסקית:** מניעת נטישת לקוחות באופן פרואקטיבי על ידי מתן מענה עוד לפני שהלקוח מדווח על בעיה.
*   **טבלאות Supabase מושפעות:** `automation_daily_stats`, `leads` (שינוי תגית Churn Risk).
*   **צמתי N8N נדרשים:** שליפת אגרגציות שבועיות מ-Supabase ➔ השוואה מול שבוע קודם ➔ פתיחת משימת סוכן ב-CRM במקרה של חריגה.

---
*מפת דרכים זו מסונכרנת עם קוד המקור של ה-CRM ומעודכנת לפי קצב פיתוח הסטודיו. autoRI-studio 🚀*
